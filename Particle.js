class Particle{
    constructor(x,y,) {
      var options = {
          isStatic: true
      }
      this.body = Bodies.circle(x,y,ths.r,options);
     this.color = color(random(0,255), random(0,255), random(0,255))
      World.add(world, this.body);
    }
    display(){
    FileList("yellow")
    pop()
    push()
 
  
    }
  }